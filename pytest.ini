[pytest]
# Nenhum cache em disco dentro da pasta do projeto: ela é sincronizada pelo
# Google Drive (seção 11.1 da especificação). `-p no:cacheprovider` evita a
# criação de `.pytest_cache`; `conftest.py` evita `__pycache__`.
addopts = -p no:cacheprovider
